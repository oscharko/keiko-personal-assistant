# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: keiko-personal-assistant
metadata:
  template: keiko-personal-assistant

services:
  backend:
    project: ./app/backend
    language: python
    host: containerapp

